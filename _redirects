# Netlify SPA fallback to prevent client-route 404s
# This ensures that all routes that don't match static files fall back to index.html
# This is essential for headless storefronts to handle client-side routing

# API routes should be handled by Netlify Functions (already configured)
/.netlify/functions/* /.netlify/functions/:splat 200

# Static assets should be served directly
/assets/* /assets/:splat 200
/data/* /data/:splat 200
/images/* /images/:splat 200
/LOGO/* /LOGO/:splat 200

# HTML pages should be served directly
/index.html /index.html 200
/collection.html /collection.html 200
/product.html /product.html 200
/about.html /about.html 200
/contact.html /contact.html 200
/privacy.html /privacy.html 200
/terms.html /terms.html 200

# All other routes fall back to index.html for SPA behavior
/* /index.html 200
